rules:
  - pattern: "go test"
    response: |
      Use "make test" instead for TDD integration:
      - make test                        # Run all tests
      - make test PKG=./internal/claude  # Test only a specific package
      See Makefile for more information.
  - pattern: "gci|go vet|goimports|gofumpt|go fmt"
    response: Use "make lint-fix" instead to resolve lint/formatting issues.
  - pattern: "cd /tmp"
    response: Create a "tmp" directory in the project root instead.

# TODO: post tool "not related to my changes"
# TODO: convert pattern matches back to regex
# TODO: some templating, like inserting file contents or shell output
# TODO: "reminders" list that adds context at start of session
# TODO: guided cli setup for adding rules
# TODO: block_paths or something to stop editing
# TODO: get rid of completion command