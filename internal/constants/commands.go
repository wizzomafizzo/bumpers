package constants

// CommandPrefix is the prefix used to identify command prompts in UserPromptSubmit events.
// Commands are identified by this prefix followed by a number (e.g., "%0", "%1").
const CommandPrefix = "%"
