rules:
    - generate: "off"
      match: rm -rf /
      tool: ^Bash$
      send: Use safer alternatives
    - generate: once
      match: rm -rf /tmp
      tool: ^Bash$
      send: Use safer alternatives
    - generate: "off"
      match: rm -rf /
      tool: ^Bash$
      send: Use safer alternatives
    - generate: once
      match: rm -rf /tmp
      tool: ^Bash$
      send: Use safer alternatives
    - generate: "off"
      match: rm -rf /
      tool: ^Bash$
      send: Use safer alternatives
    - generate: once
      match: rm -rf /tmp
      tool: ^Bash$
      send: Use safer alternatives
    - generate: "off"
      match: rm -rf /
      tool: ^Bash$
      send: Use safer alternatives
    - generate: once
      match: rm -rf /tmp
      tool: ^Bash$
      send: Use safer alternatives
    - generate: "off"
      match: rm -rf /
      tool: ^Bash$
      send: Use safer alternatives
    - generate: once
      match: rm -rf /tmp
      tool: ^Bash$
      send: Use safer alternatives
    - generate: "off"
      match: rm -rf /
      tool: ^Bash$
      send: Use safer alternatives
    - generate: once
      match: rm -rf /tmp
      tool: ^Bash$
      send: Use safer alternatives
    - generate: "off"
      match: rm -rf /
      tool: ^Bash$
      send: Use safer alternatives
    - generate: once
      match: rm -rf /tmp
      tool: ^Bash$
      send: Use safer alternatives
    - generate: "off"
      match: rm -rf /
      tool: ^Bash$
      send: Use safer alternatives
    - generate: once
      match: rm -rf /tmp
      tool: ^Bash$
      send: Use safer alternatives
    - generate: "off"
      match: rm -rf /
      tool: ^Bash$
      send: Use safer alternatives
    - generate: once
      match: rm -rf /tmp
      tool: ^Bash$
      send: Use safer alternatives
    - generate: "off"
      match: rm -rf /
      tool: ^Bash$
      send: Use safer alternatives
    - generate: once
      match: rm -rf /tmp
      tool: ^Bash$
      send: Use safer alternatives
    - generate: "off"
      match: rm -rf /
      tool: ^Bash$
      send: Use safer alternatives
    - generate: once
      match: rm -rf /tmp
      tool: ^Bash$
      send: Use safer alternatives
    - generate: "off"
      match: rm -rf /
      tool: ^Bash$
      send: Use safer alternatives
    - generate: once
      match: rm -rf /tmp
      tool: ^Bash$
      send: Use safer alternatives
    - generate: "off"
      match: rm -rf /
      tool: ^Bash$
      send: Use safer alternatives
    - generate: once
      match: rm -rf /tmp
      tool: ^Bash$
      send: Use safer alternatives
    - generate: "off"
      match: rm -rf /
      tool: ^Bash$
      send: Use safer alternatives
    - generate: once
      match: rm -rf /tmp
      tool: ^Bash$
      send: Use safer alternatives
    - generate: "off"
      match: rm -rf /
      tool: ^Bash$
      send: Use safer alternatives
    - generate: once
      match: rm -rf /tmp
      tool: ^Bash$
      send: Use safer alternatives
    - generate: "off"
      match: rm -rf /
      tool: ^Bash$
      send: Use safer alternatives
    - generate: once
      match: rm -rf /tmp
      tool: ^Bash$
      send: Use safer alternatives
    - generate: "off"
      match: rm -rf /
      tool: ^Bash$
      send: Use safer alternatives
    - generate: once
      match: rm -rf /tmp
      tool: ^Bash$
      send: Use safer alternatives
    - generate: "off"
      match: rm -rf /
      tool: ^Bash$
      send: Use safer alternatives
    - generate: once
      match: rm -rf /tmp
      tool: ^Bash$
      send: Use safer alternatives
    - generate: "off"
      match: rm -rf /
      tool: ^Bash$
      send: Use safer alternatives
    - generate: once
      match: rm -rf /tmp
      tool: ^Bash$
      send: Use safer alternatives
    - generate: "off"
      match: rm -rf /
      tool: ^Bash$
      send: Use safer alternatives
    - generate: once
      match: rm -rf /tmp
      tool: ^Bash$
      send: Use safer alternatives
    - generate: "off"
      match: rm -rf /
      tool: ^Bash$
      send: Use safer alternatives
    - generate: once
      match: rm -rf /tmp
      tool: ^Bash$
      send: Use safer alternatives
    - generate: "off"
      match: rm -rf /
      tool: ^Bash$
      send: Use safer alternatives
    - generate: "off"
      match: rm -rf /
      tool: ^Bash$
      send: Use safer alternatives
    - generate: once
      match: rm -rf /tmp
      tool: ^Bash$
      send: Use safer alternatives
    - generate: "off"
      match: rm -rf /
      tool: ^Bash$
      send: Use safer alternatives
    - generate: once
      match: rm -rf /tmp
      tool: ^Bash$
      send: Use safer alternatives
    - generate: once
      match: rm -rf /tmp
      tool: ^Bash$
      send: Use safer alternatives
    - generate: once
      match: rm -rf /tmp
      tool: ^Bash$
      send: Use safer alternatives
    - generate: once
      match: rm -rf /tmp
      tool: ^Bash$
      send: Use safer alternatives
    - generate: once
      match: rm -rf /tmp
      tool: ^Bash$
      send: Use safer alternatives
    - generate: "off"
      match: rm -rf /
      tool: ^Bash$
      send: Use safer alternatives
    - generate: "off"
      match: rm -rf /
      tool: ^Bash$
      send: Use safer alternatives
    - generate: "off"
      match: rm -rf /
      tool: ^Bash$
      send: Use safer alternatives
    - generate: "off"
      match: rm -rf /
      tool: ^Bash$
      send: Use safer alternatives
    - generate: once
      match: rm -rf /tmp
      tool: ^Bash$
      send: Use safer alternatives
    - generate: once
      match: rm -rf /tmp
      tool: ^Bash$
      send: Use safer alternatives
    - generate: "off"
      match: rm -rf /
      tool: ^Bash$
      send: Use safer alternatives
    - generate: once
      match: rm -rf /tmp
      tool: ^Bash$
      send: Use safer alternatives
    - generate: once
      match: rm -rf /tmp
      tool: ^Bash$
      send: Use safer alternatives
    - generate: once
      match: rm -rf /tmp
      tool: ^Bash$
      send: Use safer alternatives
    - generate: "off"
      match: rm -rf /
      tool: ^Bash$
      send: Use safer alternatives
    - generate: "off"
      match: rm -rf /
      tool: ^Bash$
      send: Use safer alternatives
    - generate: once
      match: rm -rf /tmp
      tool: ^Bash$
      send: Use safer alternatives
    - generate: once
      match: rm -rf /tmp
      tool: ^Bash$
      send: Use safer alternatives
    - generate: "off"
      match: rm -rf /
      tool: ^Bash$
      send: Use safer alternatives
    - generate: "off"
      match: rm -rf /
      tool: ^Bash$
      send: Use safer alternatives
    - generate: once
      match: rm -rf /tmp
      tool: ^Bash$
      send: Use safer alternatives
    - generate: "off"
      match: rm -rf /
      tool: ^Bash$
      send: Use safer alternatives
    - generate: "off"
      match: rm -rf /
      tool: ^Bash$
      send: Use safer alternatives
    - generate: once
      match: rm -rf /tmp
      tool: ^Bash$
      send: Use safer alternatives
    - generate: "off"
      match: rm -rf /
      tool: ^Bash$
      send: Use safer alternatives
    - generate: once
      match: rm -rf /tmp
      tool: ^Bash$
      send: Use safer alternatives
    - generate: "off"
      match: rm -rf /
      tool: ^Bash$
      send: Use safer alternatives
    - generate: "off"
      match: rm -rf /
      tool: ^Bash$
      send: Use safer alternatives
    - generate: "off"
      match: rm -rf /
      tool: ^Bash$
      send: Use safer alternatives
    - generate: once
      match: rm -rf /tmp
      tool: ^Bash$
      send: Use safer alternatives
    - generate: once
      match: rm -rf /tmp
      tool: ^Bash$
      send: Use safer alternatives
    - generate: once
      match: rm -rf /tmp
      tool: ^Bash$
      send: Use safer alternatives
    - generate: once
      match: rm -rf /tmp
      tool: ^Bash$
      send: Use safer alternatives
    - generate: once
      match: rm -rf /tmp
      tool: ^Bash$
      send: Use safer alternatives
    - generate: once
      match: rm -rf /tmp
      tool: ^Bash$
      send: Use safer alternatives
